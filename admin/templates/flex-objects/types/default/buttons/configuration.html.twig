{% if configure_url and user.authorize(directory.config('admin.configure.authorize') ?? 'admin.super') %}
<a class="button" href="{{ configure_url }}">
    <i class="fa fa-cog"></i> {{ 'PLUGIN_ADMIN.CONFIGURATION'|tu }}
</a>
{% endif %}
