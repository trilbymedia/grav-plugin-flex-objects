{% if child_url %}
<a class="button" href="{{ child_url }}" title="{{ title }}">
    <i class="fa fa-chevron-down"></i>
</a>
{% else %}
<span class="button disabled">
    <i class="fa fa-chevron-down"></i>
</span>
{% endif %}
