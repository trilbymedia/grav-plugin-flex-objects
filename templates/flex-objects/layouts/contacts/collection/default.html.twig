<div id="flex-objects">
    <ul class="list">
        {% for object in collection %}
            {% if object.published %}
            <li>
                {{ object.render(layout)|raw }}
            </li>
        {% endif %}
        {% endfor %}
    </ul>
</div>
