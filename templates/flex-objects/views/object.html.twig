{% set flex = grav.flex_objects %}
{% set directory = flex.directory(type) %}
{% set object = directory.getObject(id) %}

{% if object %}
    {% render object layout: options.layout ?: 'default' %}
{% else %}
    <h2>Entry Not Found</h2>
{% endif %}