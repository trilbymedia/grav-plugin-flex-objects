{% set flex = grav.flex_directory %}
{% set directory = flex.directory(type) %}
{% set type = directory.type %}

<h2>{{ type|capitalize }}</h2>

{% include ['flex-directory/types/' ~ type ~ '.html.twig', 'flex-directory/types/basic.html.twig'] %}
